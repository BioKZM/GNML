import 'package:flutter/material.dart';

class UIConstants {
  // Sidebar
  static const double leftSidebarExpandedWidth = 250.0;
  static const double leftSidebarCollapsedWidth = 60.0;
  static const double rightSidebarWidth = 300.0;

  // Padding
  static const double standardPadding = 16.0;
  static const double profilePadding = 16.0;
  static const double contentCardPadding = 8.0;

  // Colors
  static const Color ratingColorStart = Colors.redAccent;
  static const Color ratingColorEnd = Colors.greenAccent;

  static const Color sidebarBackgroundColor =
      Color(0xFF1E1E1E);
  static const Color contentBackgroundColor =
      Color(0xFF121212);
}
